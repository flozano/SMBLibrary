name: Build and Publish

on:
  push:
    branches:
      - '**'
    tags:
      - 'v*.*.*'
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Restore
        run: dotnet restore SMBLibrary/SMBLibrary.csproj

      - name: Build
        run: dotnet build SMBLibrary/SMBLibrary.csproj -c Release --no-restore

      - name: Test
        run: dotnet test SMBLibrary.Tests/SMBLibrary.Tests.csproj --no-restore

      - name: Pack
        run: dotnet pack SMBLibrary/SMBLibrary.csproj -c Release -o ./artifacts --no-build

      - name: Publish artifacts
        uses: actions/upload-artifact@v4
        with:
          name: packages
          path: artifacts/*.nupkg

      - name: Publish to NuGet
        if: startsWith(github.ref, 'refs/tags/v')
        env:
          NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
        run: |
          if [ -z "$NUGET_API_KEY" ]; then
            echo "NUGET_API_KEY secret is not set. Skipping publish."
            exit 0
          fi
          for package in artifacts/*.nupkg; do
            dotnet nuget push "$package" --api-key "$NUGET_API_KEY" --source https://api.nuget.org/v3/index.json
          done
